{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core defaults: window + event (listen) for the main window. App commands (submit/approve/answer/abort/set_mode) are not gated by capabilities.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
