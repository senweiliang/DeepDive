# <功能名 / 机制名> spec

<!--
单元 spec.md 模板。HEAD 文档：永远只反映当前形态，历史归 git / adr。
产生触发器（任一）：① nav 卡事实超约 10 行 ② 出过事故 / 被误解 ③ 跨端 / 多文件耦合 ④ 第二个任务碰到本单元。
写作纪律：只写代码表达不了的事实（why / 不变量 / 契约 / 跨文件耦合）；代码能表达的（签名 / 枚举值 / 字段）写「详见 `Xxx`」。
代码事实（路径 / 类名 / 字段）一律用反引号——check.py 靠它抓锚点校验。
-->

## 行为契约

<语义 / 输入输出 / schema。只写代码表达不了的；代码能表达的留指针「详见 `Xxx`」。>

## 用户场景

<谁在什么场景用、典型输入输出、验收口径、场景约束（场景约束即不变量）。>

## 不变量

<每条一句，尽量写成 audit 可核对的可判定陈述（如「状态恰好 5 种」，而非「状态有很多种」）。>

## 落点与验证

<!-- 下面 `- <repo路径>…` 行是 staleness.py 的解析对象，格式勿改（行首 `- ` + 反引号包裹的源文件路径）。 -->

- `src/.../<Xxx>` — <职责一句话>
- 修改清单：改 <某字段 / 行为> 要同步 <跨文件对齐点清单>
- 验证：<单测 target / 可运行 fixture / 手工验证路径>

<!-- 尾部核对锚点：codemap-audit 核对无差异时只更新这一行的 commit，不产生噪音提交。 -->
<!-- verified: <commit-hash> -->
