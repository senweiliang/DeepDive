# <单元> 红线

<!--
单元 attention.md 模板。事故触发：红线**被踩过**（事故证明读 spec 拦不住人），或 spec 长到快修任务读不起。
不预写。默认不建此文件——红线以不变量身份躺在 spec.md 里即可。
准入：只收「一行禁止句 + 指针」。解释 / 完整契约 / 原理一律在 spec / adr，链过去。
允许一句话的重复（禁止句 + 指针），不允许一段话的重复。
预算：≤15 行（check.py warn）。
-->

- 禁止 <X>，会炸 <Y> → 详见 [spec.md#不变量](spec.md) / [adr/NNNN](adr/NNNN-xxx.md)
