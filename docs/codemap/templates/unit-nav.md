# <功能名 / 机制名>

<!--
单元 nav.md 模板（feature/<x>/ 或 design/<x>/ 的强制入口）。
职责：门牌（被找到）+ 闭包（触发条件 → 读什么）。本文件不装事实，事实在 spec.md。
预算：≤30 行（check.py warn）。膨胀即抽 spec.md，nav 回缩成纯门牌。
相对路径按单元位置：feature/<x>/ · design/<x>/ 用 ../../ 上行到模块根；更深一层（如 feature/nodes/<x>/）多加一级 ../。
-->

**一句话**：<做什么 + 最关键的行为特征>。
**标识**：`<类型 / 分类 / 主入口标识符>`      ← 机制类（design）可省
**关键词**：<用户会当 prompt 第一个词说的词 / 黑话 / 项目代号 / 类名 / 字段 / 错误码，斜杠分隔——命中即路由到本页>

## 上下文闭包（触发条件 → 读什么）

<!-- 每条 = 「什么情况下 → 读哪篇」。这是 prompt 驱动分发的唯一机制。宁可多列一条依赖，别漏。
     只用关系词表里的关系：机制依赖 / 数据契约 / 复用链路 / 同源功能 / 设计依据 / 必读红线 / 范围边界。 -->

- 改行为语义 / 参数 schema → [spec.md](spec.md)
- 碰 <某类操作，如写库 / 某字段> → 必读 [<拥有单元>/attention.md](<相对路径>)
- 想改 <某现状设计> → 先读 [adr/NNNN](<相对路径>)（当年的取舍）
- 机制依赖：[../../design/<机制>/](<相对路径>)（一句话说明为什么依赖）
- 复用链路：<复用了谁的实现，链过去>
- 源码：`<repo相对路径>/<X>.<ext>`
- 范围边界：<仅当发生过走错门才写：具体误解 + 正确去向；否则整条省略>

## 详情

spec.md — ⏳ 待补 / ✅ 已建
