# NNNN. <短标题>

<!--
ADR 模板。文件名 NNNN-短标题-kebab.md，编号四位单调递增。
准入：三条 **AND** 都满足才写（否则不写）——
  1. 难逆 / 高变更成本：改回去要重构大量核心代码。
  2. 缺上下文会让人意外：未来的人看到会问「为什么不用更主流的 X」。
  3. 存在真权衡：不是对错题，是为满足某指标（性能 / 隔离 / 离线…）放弃了另一部分便利。
预期频率：一个模块一年几条。显然的 / 跟随惯例的决策不写。
读取时机：① 闭包「设计依据」点名 ② 推翻现状前必查。
与规范层分工：「以后都这么做」→ CLAUDE.md / conventions（每次都读到）；「当时为什么这么定」→ 本文件（按需读）。
-->

- **状态**：Accepted ｜ Superseded by NNNN
- **日期**：YYYY-MM-DD

## 背景

<当时面对的问题 / 约束 / 指标。>

## 决策

<选了什么。>

## 被放弃的方案

<方案 B（表面上看起来更合理或至少同样合理的那个）+ 为什么否决它。这段是 ADR 的核心。>

## 后果

<接受了什么代价 / 换来了什么。谁将来想翻案要先知道的东西。>
